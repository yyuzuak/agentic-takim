"""Builder — v2.1 Workspace Runtime.

artifact → doğrulanmış repo: assemble + validate + persist + snapshot.
Gerçek exec (npm install/build) YAPMAZ — o v2.2 services/sandbox'a aittir (kesin sınır).
"""

ASSEMBLER_VERSION = "2.1.0"
